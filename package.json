{
  "name": "scope-map",
  "version": "1.4.2",
  "description": "define a tree of scopes, creating scope strings to provide access without redundancy, and matching agains required scopes",
  "main": "src/index.js",
  "scripts": {
    "build": "mkdir build -p && nearleyc grammer/layout.ne -o build/layout.js",
    "prepare": "npm run build",
    "test": "npm run build && mocha"
  },
  "author": "Leyla Becker",
  "license": "MIT",
  "dependencies": {
    "moo": "^0.5.1",
    "nearley": "^2.20.1"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/jan-leila/scope-map.git"
  },
  "keywords": [
    "scope",
    "oauth2",
    "scope-map"
  ],
  "devDependencies": {
    "mocha": "^9.1.3"
  }
}
